{% extends 'base.html.twig' %}

{% block title %}Card{% endblock %}

{% block body %}
<h1>Card</h1>

<div class="card-deck">
    {% for card in cards %}
        {{ card.getAsHTML()|raw }}
    {% endfor %}
</div>

<p><a href="{{ path('card_start') }}">Back to homepage</a></p>
{% endblock %}